# Frontend (React + Vite)

## Development

From `frontend/`:

```bash
npm install
npm run dev
```

## Environment

Copy the example env file and point it at your backend API:

```bash
cp .env.example .env
```

## Build

```bash
npm run build
```
